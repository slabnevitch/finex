import config from "@/config"

// const VARIABLE_NAME = null
// const DOMAIN = (config.PARAMS.BACKEND_IS_DEVELOPMENT === true) ? 'http://backend.fin-expert.loc/' : 'http://api.beta.finexpert.online/';

const DOMAIN = 'https://backend.finexpert.online/';

export default {
  // VARIABLE_NAME
  DOMAIN
}
